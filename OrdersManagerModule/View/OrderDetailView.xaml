<UserControl x:Class="OrdersManagerModule.View.OrderDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Text="Ordered :" />
        <DatePicker Grid.Column="1" Grid.Row="0" SelectedDate="{Binding Path=DateOrdered}" Width="240" />

        <TextBlock Grid.Column="0" Grid.Row="1" Text="Content :"/>
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding Path=Content}" Width="240" Height="50" TextWrapping="Wrap" AcceptsReturn="True" />

        <TextBlock Grid.Column="0" Grid.Row="2" Text="Received :" />
        <DatePicker Grid.Column="1" Grid.Row="2" SelectedDate="{Binding Path=DateReceived}" Width="240" />

        <TextBlock Grid.Column="0" Grid.Row="3" Text="Status :" />
        <ComboBox Grid.Column="1" Grid.Row="3" SelectedValue="{Binding Path=Status}" ItemsSource="{Binding EnumCol}" Width="240"/>

        <TextBlock Grid.Column="0" Grid.Row="4" Text="Receiver :" />
        <ComboBox Grid.Column="1" Grid.Row="4" ItemsSource="{Binding AllClient, Mode=TwoWay}" SelectedItem="{Binding Path=Receiver, Mode=TwoWay}" Width="240">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding name}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        


        <Button Content="Validate" Command="{Binding ValidateOrderCommand}" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" Grid.Row="5" Width="150"/>
    </Grid>
</UserControl>

<UserControl x:Class="UserManagerModule.View.UserDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:UserManagerModule.View"
             xmlns:vm="clr-namespace:UserManagerModule.ViewModel"
             mc:Ignorable="d" d:DesignWidth="300" Height="147.467">

    <Border BorderBrush="#41B1E1" BorderThickness="2">
     <Grid Margin="5">

        <TextBlock Text="Name:" Margin="0,8,225,110.2" />
        <TextBox  Text="{Binding Path=name}" Margin="81,5,61,107.2" TextChanged="TextBox_TextChanged" FontSize="10" />

        <TextBlock  Text="Last Name:" Margin="0,43,225,75.2" />
        <TextBox  Text="{Binding Path=last_name}" Margin="80,39,62,72.2" TextChanged="TextBox_TextChanged_1" FontSize="10" />

        <TextBlock  Text="Login:" Margin="0,80,225,38.2" />
        <TextBox  Text="{Binding Path=login}" Margin="80,75,62,36.2" FontSize="10"/>

        <TextBlock  Text="Password:" Margin="0,113,225,5.2" />
        <TextBox  Text="{Binding Path=pwd}" Margin="81,109,61,3.2" FontSize="10" />
    </Grid>
    </Border>
</UserControl>

<UserControl x:Class="BMSModule.View.MainGModulesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:BMSModule.ViewModel"
      xmlns:v="clr-namespace:BMSModule.View"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:UserListViewModel}">
            <v:UserListView />
        </DataTemplate>
    </UserControl.Resources>
    <Grid Margin="4">
        <Border Background="GhostWhite">
            <ItemsControl ItemsSource="{Binding ViewModels}" Margin="4" />
        </Border>
    </Grid>
</UserControl>

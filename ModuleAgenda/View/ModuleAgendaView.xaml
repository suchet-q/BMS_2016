<UserControl x:Class="ModuleAgenda.View.ModuleAgendaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="11*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Calendar Grid.Column="1" SelectedDate="{Binding CurrentDate}" HorizontalAlignment="Left" Margin="0" VerticalAlignment="Top" Grid.RowSpan="2" Height="168" Width="179"/>
        <StackPanel Grid.Column="1" Margin="0" Grid.Row="1" Width="180"/>
        <ListView Grid.RowSpan="2" ItemsSource="{Binding AllEvents}" SelectionChanged="ListView_SelectionChanged">
            <ListView.View>
                
                <GridView>
                    <GridViewColumn Header="Titre" DisplayMemberBinding="{Binding Path=Title}"/>
                    <GridViewColumn Header="Date" DisplayMemberBinding="{Binding Path=DateString}"/>
                    <GridViewColumn Header="Debut" DisplayMemberBinding="{Binding Path=StartEvent}"/>
                    <GridViewColumn Header="Fin" DisplayMemberBinding="{Binding Path=EndEvent}"/>
                    <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Path=Description}"/>
                    <GridViewColumn Header="Lieu" DisplayMemberBinding="{Binding Path=Location}"/>
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Path=Status}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
